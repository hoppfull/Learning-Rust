fn main() {
    let t0 = (1, "hello"); // a tuple with two elements
    println!("t0=({},{})", t0.0, t0.1); // (1, hello)
    let t1 = (5,); // a tuple with one element
    println!("t1=({})", t1.0) // (5)
}
